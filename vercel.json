{
    "framework": "vite",
    "buildCommand": "CI=false npm run build",
    "outputDirectory": "dist",
    "env": {
        "CI": "false"
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}